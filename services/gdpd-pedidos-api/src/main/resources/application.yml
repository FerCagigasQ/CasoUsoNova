spring:
  application:
    name: gdpd-pedidos-api
  datasource:
    url: jdbc:postgresql://localhost:5432/gdpddb
    username: nova
    password: ${DB_PASSWORD:nova}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
  # Spring Cloud Stream: ActiveMQ via AMQP binder
  cloud:
    stream:
      bindings:
        pedidos-created-out-0:
          destination: pedidos.created
          content-type: application/json
        pedidos-updated-out-0:
          destination: pedidos.updated
          content-type: application/json
        pedidos-deleted-out-0:
          destination: pedidos.deleted
          content-type: application/json
      rabbit:
        bindings:
          pedidos-created-out-0:
            producer:
              autoBindDlq: true
              dlqTtl: 60000
              dlqDeadLetterExchange: pedidos.dlx
          pedidos-updated-out-0:
            producer:
              autoBindDlq: true
              dlqTtl: 60000
              dlqDeadLetterExchange: pedidos.dlx
          pedidos-deleted-out-0:
            producer:
              autoBindDlq: true
              dlqTtl: 60000
              dlqDeadLetterExchange: pedidos.dlx
    config:
      uri: http://localhost:8888
      name: gdpd-pedidos-api
      profile: ${spring.profiles.active:dev}
      fail-fast: true
  rabbitmq:
    host: ${BROKER_HOST:localhost}
    port: ${BROKER_PORT:5672}
    username: ${BROKER_USER:guest}
    password: ${BROKER_PASS:guest}
  sleuth:
    sampler:
      probability: 1.0
    propagation:
      type: B3
    web:
      enabled: true
    async:
      enabled: true
    messaging:
      enabled: true
  zipkin:
    base-url: http://localhost:9411
    enabled: true
    sender:
      type: web

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,loggers,env,refresh
      base-path: /actuator
  endpoint:
    health:
      show-details: when-authorized
      probes:
        enabled: true
    loggers:
      enabled: true
  health:
    db:
      enabled: true
    diskSpace:
      enabled: true
    jms:
      enabled: true
    eureka:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
        step: 30s
    distribution:
      percentiles-histogram:
        http.server.requests: true
      percentiles:
        http.server.requests: 0.5, 0.95, 0.99
      sla:
        http.server.requests: 500ms, 1s, 2s
    tags:
      application: ${spring.application.name}
      environment: ${spring.profiles.active:dev}
      version: @project.version@

info:
  app:
    name: ${spring.application.name}
    description: "Servicio GDPD - API REST de Gestión de Pedidos"
    version: @project.version@
    build-time: @maven.build.timestamp@
  java:
    version: ${java.version}
  spring:
    boot-version: @spring-boot.version@

logging:
  pattern:
    level: "%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]"

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
