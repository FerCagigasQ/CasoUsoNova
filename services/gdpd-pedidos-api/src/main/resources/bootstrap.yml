spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: gdpd-pedidos-api
      profile: ${spring.profiles.active:dev}
      fail-fast: true
      retry:
        max-attempts: 5
        initial-interval: 1000
